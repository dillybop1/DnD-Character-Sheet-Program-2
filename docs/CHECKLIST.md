# Running Checklist

| Task ID | Status | Milestone | Description | Dependencies | Verification Command | Exit Criteria |
| --- | --- | --- | --- | --- | --- | --- |
| M0-01 | done | M0 | Bootstrap repo, tracking docs, README workflow, and local git init | None | `git status --short && npm run typecheck && npm run test && npm run build` | Repo has committed handoff docs, a local `main` branch, and verified bootstrap commands |
| M1-01 | done | M1 | Scaffold Electron + React + TypeScript app, renderer shell, and packaging baseline | M0-01 | `npm run build` | Desktop app builds successfully and emits a macOS DMG |
| M1-02 | done | M1 | Provide a stable local live development flow behind `npm run dev` | M1-01 | `npm run dev` | Vite starts cleanly on `localhost:5173` and the app uses a browser-backed `dndApi` mock for HMR development |
| M1-03 | blocked | M1 | Investigate a direct Electron-shell hot-run path beyond the browser dev shim | M1-01 | `./node_modules/electron/dist/Electron.app/Contents/MacOS/Electron .` | Native Electron dev boot no longer resolves `require('electron')` to the installed binary path |
| M2-01 | done | M2 | Add SQLite persistence, migration runner, preload bridge, and starter local CRUD | M1-01 | `npm run typecheck && npm run test` | Characters, homebrew entries, and compendium seed data persist locally through the IPC surface |
| M2-02 | done | M2 | Add source-aware content architecture so future rulebooks can be installed as separate packages | M2-01 | `npm run typecheck && npm run test && npm run build` | Characters, templates, and compendium entries all carry source metadata and filter through enabled source profiles |
| M3-01 | done | M3 | Replace starter compendium seed with fuller SRD/open ingestion pipeline | M2-01 | `npm run typecheck && npm run test && npm run build` | Seed/import workflow can load a materially larger compendium dataset and sync it idempotently into local storage |
| M3-02 | done | M3 | Link sheet and builder surfaces into the richer compendium so rules references open from character context | M3-01, M6-01 | `npm run dev && npm run typecheck && npm run build` | Core sheet/resource surfaces can open matching compendium entries without leaving the character workflow blind |
| M4-01 | done | M4 | Add starter rules engine coverage for core sheet math | M2-01 | `npm run test` | Unit tests cover fighter armor math and wizard spellcasting progression |
| M5-01 | done | M5 | Implement guided builder, character library, and editable sheet preview | M2-01, M4-01 | `npm run typecheck && npm run build` | A character can be created, saved, reloaded, edited, deleted, and previewed with live derived values |
| M5-02 | done | M5 | Split the character workflow into a roster landing page, a dedicated sheet page, and a creator/editor route with live preview inside the same window | M5-01, M6-01 | `npm run typecheck && npm run test && npm run build` | App launch lands on a roster-first home page, opening an existing character navigates to a dedicated in-app sheet view, creating or editing a character navigates to a creator/editor page with live preview, and no second native window is required |
| M4-02 | done | M4 | Extend rules engine for more classes, spell progression edge cases, and overrides | M4-01 | `npm run typecheck && npm run test && npm run build` | Core derived math covers the intended v1 classes and expected edge cases |
| M6-01 | done | M6 | Rebuild the sheet preview to match the reference page's structural layout with live derived bindings | M5-01 | `npm run typecheck && npm run build` | The sheet uses the same major panel hierarchy as the reference while remaining driven by current app data |
| M6-02 | done | M6 | Refine ornamental detail, sheet readability, tracked vitals, spacing, and print behavior into a print-perfect final sheet | M6-01, M5-02 | `npm run typecheck && npm test && npm run build` | The sheet styling, list/detail readability, tracked vitals, masthead/panel density, pagination, and export output are polished enough to ship as the final presentation layer |
| M7-01 | done | M7 | Deepen spell preparation and homebrew application beyond the starter workflow | M5-01, M4-02 | `npm run typecheck && npm run test && npm run build` | The character editor uses class-filtered spell lists, cleaner prepared-spell behavior, and a multi-effect bounded homebrew editor |
| M7-02 | done | M7 | Deepen inventory and equipped-item management beyond the basic weapon/armor checklist flow | M5-01, M7-01 | `npm run typecheck && npm run test && npm run lint && npm run build` | The character editor supports richer equipment state and surfaces more inventory-derived detail than the current starter loadout |
| M7-03 | done | M7 | Expand equipment/reference coverage and item-specific detail beyond the starter inventory model | M7-02 | `npm run typecheck && npm run test && npm run lint && npm run build` | The compendium and builder can represent more than the starter gear set with useful item-level detail and references |
| M7-04 | done | M7 | Promote subclasses to first-class compendium content and derived sheet summaries | M7-01, M3-02 | `npm run typecheck && npm run test && npm run lint && npm run build:win-local` | Subclass selections normalize to seeded compendium entries, the compendium can browse/filter them, and the sheet folds subclass feature summaries into linked character context |
| M7-05 | done | M7 | Promote feats and background features to structured character data and route homebrew summaries into the matching sheet sections | M7-04, M3-02 | `npm run typecheck && npm run test && npm run lint && npm run build:win-local` | Feats are selectable as canonical compendium entries, background features persist on the character/sheet, and homebrew feat/feature/species-trait names render in the correct sheet panels |
| M7-06 | done | M7 | Add starter feat mechanics for the seeded feat set where the rules can be represented cleanly | M7-05, M4-02 | `npm run typecheck && npm run test && npm run lint && npm run build:win-local` | `Alert` and `Tough` change derived math automatically, while `Magic Initiate` stays clearly marked as reference-only until spell-choice UX exists |
| M7-07 | done | M7 | Add bounded `Magic Initiate` spell selection support for non-casters | M7-06, M7-05 | `npm run typecheck && npm run test && npm run lint && npm run build:win-local` | Non-caster characters can choose a supported spell list plus feat-granted spells for `Magic Initiate`, and those spells flow into derived spell output without pretending to support multi-ability casters yet |
| M7-08 | done | M7 | Promote background guidance into shared templates and let the builder apply starting gear into inventory | M7-05, M7-02 | `npm run typecheck && npm run test && npm run lint && npm run build:win-local` | Selected backgrounds expose seeded themes, suggested skills, and idempotent starting gear that can be merged into tracked inventory without duplicating items |
| M7-09 | done | M7 | Add bounded configurable feat choices for starter feats that fit the current rules engine | M7-06, M7-05 | `npm run typecheck && npm run test && npm run lint && npm run build:win-local` | Characters persist bounded per-feat choices, and starter feats such as `Skilled` and `Resilient` apply their chosen skills/ability bonuses through the derived rules engine |
| M7-10 | done | M7 | Generalize configurable feat choices to support multi-group feats and add `Skill Expert` | M7-09 | `npm run typecheck && npm run test && npm run lint && npm run build:win-local` | Feats can expose multiple independent choice groups per feat, and `Skill Expert` applies its chosen ability increase, new proficiency, and expertise through the current rules engine |
| M7-11 | done | M7 | Surface selected configurable feat choices in the sheet presentation layer | M7-10, M6-01 | `npm run typecheck && npm run test && npm run lint && npm run pack:win-local` | The sheet preview shows the selected configurable feat choices alongside the feat name so `Skilled`, `Resilient`, and `Skill Expert` remain legible outside the builder UI |
| M7-12 | done | M7 | Add feat-level legality checks for bounded configurable feats and self-heal stale invalid selections | M7-11, M7-10 | `npm run typecheck && npm run test && npm run lint && npm run pack:win-local` | The feat picker disables impossible configurable feats, and builder/save/sheet paths all remove feat ids or selections that the current class/skill state can no longer satisfy |
| M7-13 | done | M7 | Broaden the starter feat catalog with more bounded configurable or partially automated feat support | M7-12 | `npm run typecheck && npm run test && npm run lint && npm run build:win-local` | More seeded feats are available with honest automation/support messaging and clean derived effects where the current rules engine can represent them safely |
| M7-14 | done | M7 | Add passive skill support to the derived sheet model and use it to deepen partial automation for `Observant` | M7-13 | `npm run typecheck && npm run test && npm run lint && npm run build:win-local` | Passive skills are derived and surfaced on the sheet, `Observant` grants its +1 ability plus passive Investigation / Perception bonuses automatically, and the remaining lip-reading benefit stays clearly reference-only |
| M7-15 | done | M7 | Deepen the remaining starter spell/feat gap by exploring a bounded multi-spellcasting model for caster-class `Magic Initiate` | M7-14, M7-07 | `npm run typecheck && npm run test && npm run lint && npm run build:win-local` | The app can represent `Magic Initiate` on caster classes honestly by keeping a separate feat spellcasting line only when the chosen list uses a different ability, while non-caster behavior stays intact |
| M7-16 | done | M7 | Broaden the bounded homebrew editor to expose safe effect types the rules engine already supports | M7-15, M7-14 | `npm run typecheck && npm run test && npm run lint && npm run build:win-local` | The homebrew editor can create initiative bonuses and per-level hit point bonuses in addition to the earlier bounded effects, and regression coverage proves those effects change derived math correctly |
| M7-17 | done | M7 | Add character JSON import so exported characters can round-trip back into the app and move between machines | M7-16, M5-01 | `npm run typecheck && npm run test && npm run lint && npm run build:win-local` | The builder/library UI can import either the app's wrapped JSON export format or a raw character record, persist it locally, and immediately load it as the active character |
| M8-01 | done | M8 | Validate packaged macOS and Windows installers, including Windows reinstall/upgrade persistence | M1-01 | `npm run build:win-local` | Installers launch, preserve local data on both target platforms, and survive JSON export/import, PDF export, and reinstall/upgrade round-trips |
| M9-01 | done | M9 | Create release-readiness docs, `v0.1.0` candidate notes, and a publish checklist | M8-01 | `npm run typecheck && npm run test && npm run build` | The repo has a concrete release handoff doc with current version, validated scope, build commands, artifact expectations, and explicit release gaps instead of implicit tribal knowledge |
| M9-02 | done | M9 | Resolve the macOS notarization/signing path and Windows signing strategy by choosing an unsigned private-beta distribution path | M9-01 | `npm run build && npm run build:win-local` | The release path states clearly that `v0.1.0` is a private beta that ships unsigned on macOS and Windows, and the release docs explain the trust-warning tradeoff honestly |
| M9-03 | done | M9 | Tag and publish private `v0.1.0` with release notes and final installer artifacts | M9-02 | `npm run release:verify-local` | The repo is tagged, the private GitHub release is published, and the intended beta installers are distributed with the unsigned-install caveat |
| M10-01 | done | M10 | Persist spell slot tracking across the editor, saved-sheet route, imports, and sheet/export output | M5-02, M6-02, M7-17 | `npm run typecheck && npm run test && npm run lint && npm run build:win-local` | Characters store remaining standard/pact spell slots, legacy/imported records normalize safely, the editor and saved-sheet route can update that state, and the sheet/export surfaces show remaining/max slot values |
| M10-02 | done | M10 | Create repo-managed content-pack schema and importer for spells plus beasts/creatures | M3-01, M10-01 | `npm run content:build && npm run typecheck && npm run test` | Canonical spell and creature/beast data now lives under `content/packs`, the build script validates those inputs, joins keyed description files, rejects broken slugs/fields, and regenerates a deterministic merged dataset |
| M10-03 | done | M10 | Load validated pack data into compendium sync/search with typed spell and creature payloads | M10-02 | `npm run content:build && npm run typecheck && npm run test` | Browser dev and Electron both consume the generated pack dataset, spells expose richer metadata/description fields, creatures are a first-class compendium type, and search/source filtering still works |
| M10-04 | done | M10 | Extend character records with `sheetProfile` and `trackedResources` | M10-03, M10-01 | `npm run typecheck && npm run test && npm run lint` | Saved characters can persist appearance, alignment, languages, currencies/equipment notes, and generic checkbox/counter resources without breaking older records |
| M10-05 | done | M10 | Build the two-page saved-sheet shell with responsive page navigation | M10-04, M5-02 | `npm run typecheck && npm run test && npm run lint && npm run build:win-local` | The dedicated `/characters/:id` route renders a clear page-one/page-two shell with print-safe structure and responsive navigation without changing the builder route yet |
| M10-06 | done | M10 | Implement the page-1 layout and inline `Edit Sheet` mode | M10-05, M10-04 | `npm run typecheck && npm run test && npm run lint && npm run build:win-local` | Page 1 reflects the new reference-style information architecture and the saved-sheet route can edit the new bounded sheet fields inline |
| M10-07 | done | M10 | Implement the page-2 spell table and spell inspector backed by pack metadata and optional full text | M10-05, M10-03 | `npm run typecheck && npm run test && npm run lint && npm run build:win-local` | Page 2 shows spell level/range/save/casting/duration/concentration data at a glance and can reveal source-aware spell descriptions without storing that text on characters |
| M10-08 | done | M10 | Add generic tracked-resource rows and manual short-rest / long-rest UI controls | M10-06, M10-04 | `npm run typecheck && npm run test && npm run lint && npm run build:win-local` | Saved sheets can track bounded charges through checkbox/counter controls and short-rest/long-rest actions reset only explicitly configured state instead of pretending to cover every 2024 rule edge case |
| M11-01 | done | M11 | Polish saved-sheet print output and narrow-width control layout before manual QA | M10-08 | `npm run typecheck && npm run test && npm run lint && npm run build:win-local` | Saved-sheet print/PDF output no longer exposes page-level action chrome, tracked counters/pips still read clearly on paper, and narrow layouts stack resource/rest controls without obvious overflow |
| M11-02 | done | M11 | Manually validate the saved-sheet route in browser dev and a packaged Windows build | M11-01 | `npm run test && npm run dev && npm run build:win-local` | The saved-sheet smoke test passes, and the two-page saved-sheet flow, spell inspector, print/PDF output, resource rows, and short-rest/long-rest actions have all been manually exercised with any defects recorded |
| M11-03 | done | M11 | Resolve validated saved-sheet QA issues and open the next feature milestone | M11-02 | `npm run typecheck && npm run test && npm run lint && npm run build:win-local` | QA findings are either fixed or documented with explicit follow-ups, and the handoff docs point to the next feature milestone instead of an ambiguous post-redesign state |
| M12-01 | done | M12 | Reskin the on-screen saved-sheet route from dark app chrome to a light paper canvas | M11-03 | `npm run typecheck && npm run test && npm run lint && npm run build:win-local` | The sheet-preview workspace, page shells, tabs, and inline controls all render through a dedicated light paper treatment on screen while preserving the current saved-sheet behavior |
| M12-02 | done | M12 | Rework page 2 into a more worksheet-like spellbook/notes layout | M12-01 | `npm run typecheck && npm run test && npm run lint && npm run build:win-local` | The page-2 spell table, inspector rail, and profile/resource blocks read closer to the spreadsheet reference's information architecture instead of a generic card stack |
| M12-03 | done | M12 | Restore the reference-style page-1 sheet as the main saved-sheet body and remove the extra quick-overview block above it | M12-01 | `npm run typecheck && npm run test && npm run lint && npm run build:win-local` | The saved-sheet route leads page 1 with the reference-style `SheetPreview` body, the unwanted top-of-page overview block is gone, and saved-sheet controls live outside that main sheet body |
| M12-04 | done | M12 | Retune print/PDF output after the worksheet reskin lands | M12-02, M12-03 | `npm run test && npm run build:win-local` | Print/PDF output preserves the same desktop saved-sheet grid used in-app instead of collapsing into the narrow stacked fallback, honors CSS page sizing during Electron export, and still allows long page-two spell tables to paginate cleanly |
| M12-05 | deferred | M12 | Manually spot-check the latest saved-sheet PDF/print output after the post-`M12-04` regression fix | M12-04 | `npm run build:win-local` | Deferred to the final-release gate after the latest packaged spot-check showed the current PDF export still looks too janky to justify more near-term print-system iteration |
| M13-01 | done | M13 | Reopen the roadmap around non-print content/product expansion and explicitly defer further PDF/print-system work | M12-04 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The handoff docs clearly state that PDF/print polish is deferred until a final-release go/no-go pass, and active milestone work is no longer blocked on print fidelity |
| M13-02 | done | M13 | Broaden the starter open spell pack so under-served class spell lists have materially more builder/compendium coverage | M13-01, M10-03 | `npm run content:build && npm run typecheck && npm run test && npm run lint && npm run build:win-local` | The repo-managed SRD spell pack grows beyond the earlier thin starter set, class-filtered lists for Druid / Ranger / Paladin and adjacent casters gain meaningfully more options, the generated content build is regenerated, and regression coverage asserts the new spell availability |
| M13-03 | done | M13 | Broaden the starter open beast/creature pack so druid-form and creature browsing have materially better baseline coverage | M13-02, M10-03 | `npm run content:build && npm run typecheck && npm run test && npm run lint && npm run build:win-local` | The repo-managed SRD creature pack grows beyond the earlier minimal set, adds more land / swim / fly beast coverage through the current pack pipeline, regenerates the generated content build, and regression coverage asserts the new creature search and lookup availability |
| M13-04 | done | M13 | Broaden the starter open arcane/control spell baseline so Bard / Sorcerer / Warlock / Wizard lists feel materially less thin inside the existing pack workflow | M13-02, M10-03 | `npm run content:build && npm run typecheck && npm run test && npm run lint && npm run build:win-local` | The repo-managed SRD spell pack adds another bounded round of low-level arcane/control staples, materially improves Bard / Sorcerer / Warlock / Wizard list breadth without changing schemas, regenerates the generated content build, and regression coverage asserts the broader class-filtered spell availability |
| M13-05 | done | M13 | Broaden the creature pack beyond beast-only browsing so the compendium has a more useful starter enemy/reference baseline | M13-03, M10-03 | `npm run content:build && npm run typecheck && npm run test && npm run lint && npm run build:win-local` | The repo-managed SRD creature pack adds a bounded set of non-beast entries across multiple creature types, the generated content build is regenerated, and regression coverage asserts that non-beast creature search and lookup now work alongside the existing beast-only browsing slice |
| M13-06 | done | M13 | Deepen long-form spell and creature descriptions for the most-used current pack entries without changing schemas | M13-04, M13-05, M10-03 | `npm run content:build && npm run typecheck && npm run test && npm run lint && npm run build:win-local` | The repo-managed SRD pack adds another bounded batch of joined spell and creature descriptions / higher-level notes, the compendium detail view and saved-sheet spell inspector surface denser text for common entries, the generated content build is regenerated, and regression coverage asserts the new description joins |
| M13-07 | done | M13 | Add a source-text policy plus repo-visible audit/inventory for verbatim versus non-verbatim rules/reference prose | M13-06, M10-03 | `npm run content:build && npm run typecheck && npm run test && npm run lint` | The repo documents when exact official text is allowed, current non-verbatim shipped rules/reference prose is inventoried, and repo-managed pack descriptions fail `content:build` if they lack a matching text-audit entry |
| M13-08 | done | M13 | Replace the current documented SRD paraphrase descriptions with exact open SRD wording where the app intends to show official text | M13-07, M13-06 | `npm run content:build && npm run typecheck && npm run test && npm run lint && npm run build:win-local` | Joined open-content spell and creature descriptions that are meant to represent official rules text now use exact SRD wording, and the non-verbatim inventory shrinks accordingly instead of growing by accident |
| M13-09 | done | M13 | Decide whether other open static compendium surfaces should gain exact-source detail text or remain documented browse summaries | M13-08, M13-07 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md docs/NON_VERBATIM_TEXT.md` | The repo has an explicit next direction for feats, equipment, rules, and other still-static open-reference text surfaces instead of leaving the exact-text policy ambiguous outside the current spell/creature pack flow |
| M13-10 | done | M13 | Add bounded exact-source open equipment detail text without replacing the current armor/weapon/gear browse summaries | M13-09, M13-08 | `npm run typecheck && npm run test && npm run lint && npm run build:win-local` | The compendium can show exact open text for a bounded starter set of armor/weapon/gear entries through dedicated detail payload fields, while the current summary fields remain compact documented browse copy instead of turning into full rules text |
| M13-11 | done | M13 | Broaden the bounded exact-source open equipment detail pass to the remaining high-traffic starter entries | M13-10, M13-09 | `npm run typecheck && npm run test && npm run lint && npm run build:win-local` | More common armor/weapon/gear entries gain dedicated exact open `officialText` payloads, the compendium detail view continues to render long text cleanly, and the repo can defer a feats/backgrounds/rules decision until equipment coverage is less thin |
| M13-12 | done | M13 | Decide whether to keep broadening seeded equipment exact-text coverage or open the same additive detail pattern for another compendium type | M13-11, M13-09 | `git diff -- docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md docs/NON_VERBATIM_TEXT.md` | The repo has an explicit next source-text direction after the broader equipment pass, including whether remaining browse-only seeded equipment should stay summary-only or whether the next exact-text slice should move to feats, rules, backgrounds, or another surface |
| M13-13 | done | M13 | Add bounded exact-source open core-rule detail text for the seeded rules glossary entries without replacing their browse summaries | M13-12, M13-08 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The compendium can show exact open SRD wording for the current seeded `rule` entries through additive `officialText` payloads, the detail view continues to render that long text cleanly, and the browse-summary layer remains explicitly documented as non-verbatim |
| M13-14 | done | M13 | Decide whether the next additive exact-text surface should move to open backgrounds or open feats after the new core-rule pass | M13-13, M13-09 | `git diff -- docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md docs/NON_VERBATIM_TEXT.md docs/CONTENT_TEXT_POLICY.md` | The repo has an explicit post-rules exact-text direction for the remaining open static seed surfaces instead of leaving the next slice ambiguous |
| M13-15 | done | M13 | Add bounded exact-source open background detail text for the seeded background entries without replacing their browse summaries | M13-14, M13-08 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The compendium can show exact open SRD wording for the current seeded `background` entries through additive `officialText` payloads, the detail view continues to render that long text cleanly, and the shorter browse-summary/theme layer remains explicitly documented as non-verbatim |
| M13-16 | done | M13 | Decide whether open feats should gain additive exact-source detail text next or remain summary-only until a broader feat data/automation refactor | M13-15, M13-09 | `git diff -- docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md docs/NON_VERBATIM_TEXT.md docs/CONTENT_TEXT_POLICY.md docs/PLAN.md` | The repo has an explicit post-background direction for the remaining open seeded reference surfaces, especially whether feats should be treated as the next exact-text target or deferred because their support copy is tightly coupled to automation messaging |
| M13-17 | done | M13 | Decide whether to keep expanding additive exact-text surfaces beyond backgrounds/rules/equipment or shift focus back to broader content breadth and product work | M13-16, M13-15 | `git diff -- docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md docs/PLAN.md` | The repo has an explicit stop/continue point for the exact-text initiative instead of letting it sprawl indefinitely across every seeded surface |
| M13-18 | done | M13 | Broaden the creature pack again with another bounded set of common low-level non-beast SRD encounter entries | M13-17, M13-05 | `cmd /c npx -y -p node@22 -p npm@10 npm run content:build && cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The repo-managed SRD creature pack adds another bounded batch of single-size low-level non-beast entries across missing creature types, the generated content build is regenerated, and regression coverage asserts the broader creature search and lookup baseline |
| M13-19 | done | M13 | Decide whether the next breadth pass should stay on creatures or return to spells now that exact-text expansion is paused | M13-18, M13-17 | `git diff -- docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md docs/PLAN.md` | The roadmap has an explicit next breadth target instead of drifting between more creatures, more spells, and renewed exact-text work |
| M13-20 | done | M13 | Broaden the spell pack again with a bounded first tranche of common SRD level 3 staples across arcane, divine, and primal lists | M13-19, M13-04 | `cmd /c npx -y -p node@22 -p npm@10 npm run content:build && cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The repo-managed SRD spell pack opens level 3 coverage with another bounded batch of high-traffic staples, the generated content build is regenerated, and regression coverage asserts the broader spell search and class-filtered list baseline |
| M13-21 | done | M13 | Decide whether the next breadth pass should stay on spells for another level 3/utility slice or swing back to creatures after the new spell tranche | M13-20, M13-19 | `git diff -- docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md docs/PLAN.md` | The roadmap has an explicit next breadth target after the first level 3 spell pass instead of drifting between more spells, more creatures, and renewed exact-text work |
| M13-22 | done | M13 | Broaden the spell pack again with a bounded second level 3 utility/support tranche that lifts weaker divine, primal, and shared lists | M13-21, M13-20 | `cmd /c npx -y -p node@22 -p npm@10 npm run content:build && cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The repo-managed SRD spell pack adds another bounded set of level 3 utility/support staples, materially improves weaker class-filtered lists without changing schemas, regenerates the generated content build, and regression coverage asserts the broader spell lookup and class-list baseline |
| M13-23 | done | M13 | Decide whether to start a third spell-breadth slice immediately or temporarily reopen spell exact-text cleanup after user-reported non-verbatim inspect text | M13-22, M13-21 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md docs/CONTENT_TEXT_POLICY.md docs/NON_VERBATIM_TEXT.md` | The roadmap explicitly records whether the user-reported spell-description mismatch should interrupt the next breadth slice, and the source-text docs stay aligned with that choice instead of leaving the exact-text cleanup as an undocumented detour |
| M13-24 | done | M13 | Retrofit the current open spell pack so verified spell descriptions use exact joined wording and inspect surfaces prefer that text | M13-23, M13-08, M13-22 | `cmd /c npx -y -p node@22 -p npm@10 npm run content:build && cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | Current open spell entries with a verified official open description source now carry exact joined text in the pack build, inspect surfaces stop leading with non-verbatim summary/effect copy when long-form text exists, and any remaining exceptions stay documented explicitly instead of being hidden |
| M13-25 | done | M13 | Decide whether to resume spell/creature breadth work or close any remaining documented spell exact-text exceptions after the retrofit | M13-24, M13-22 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md docs/CONTENT_TEXT_POLICY.md docs/NON_VERBATIM_TEXT.md` | The roadmap has an explicit post-retrofit direction instead of drifting between another spell tranche, another creature tranche, and the small set of remaining documented exact-text gaps |
| M13-26 | done | M13 | Broaden the spell pack again with a bounded first level 4 tranche focused on high-traffic mobility, defense, tracking, and transformation staples | M13-25, M13-24, M13-22 | `cmd /c npx -y -p node@22 -p npm@10 npm run content:build && cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The repo-managed SRD spell pack opens level 4 coverage with another bounded set of high-traffic utility, defense, movement, and transformation spells, the generated content build is regenerated, and regression coverage asserts the broader spell lookup, exact joined description, and class-list baseline |
| M13-27 | done | M13 | Decide whether the next breadth pass should stay on spells for another level 4 slice or swing back to creatures after the first level 4 tranche | M13-26, M13-25 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md docs/CONTENT_TEXT_POLICY.md docs/NON_VERBATIM_TEXT.md` | The roadmap has an explicit next breadth target after the first level 4 spell pass instead of drifting between more spells, more creatures, and the remaining documented exact-text exception |
| M13-28 | done | M13 | Broaden the spell pack again with a bounded second level 4 tranche focused on battlefield control, divination, area denial, and cleanup staples | M13-27, M13-26 | `cmd /c npx -y -p node@22 -p npm@10 npm run content:build && cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The repo-managed SRD spell pack deepens level 4 coverage with another bounded set of high-traffic control, divination, and battlefield-shaping spells, the generated content build is regenerated, and regression coverage asserts the broader spell lookup, exact joined description, and class-list baseline |
| M13-29 | done | M13 | Decide whether the next breadth pass should stay on spells for one more level 4 slice or swing back to creatures after the second level 4 tranche | M13-28, M13-27 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md docs/CONTENT_TEXT_POLICY.md docs/NON_VERBATIM_TEXT.md` | The roadmap has an explicit next breadth target after the second level 4 spell pass instead of drifting between another spell tranche, a swing back to creatures, and the remaining documented exact-text exception |
| M13-30 | done | M13 | Broaden the spell pack again with a bounded third level 4 tranche focused on scouting, summoning, acid or force damage, and remaining iconic control, illusion, and transmutation spells | M13-29, M13-28 | `cmd /c npx -y -p node@22 -p npm@10 npm run content:build && cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The repo-managed SRD spell pack deepens level 4 coverage with another bounded set of high-traffic scouting, summoning, control, and damage spells, the generated content build is regenerated, and regression coverage asserts the broader spell lookup, exact joined description, and class-list baseline |
| M13-31 | done | M13 | Decide whether to finish the remaining niche level 4 spell cleanup or swing back to creatures now that the shared or iconic level 4 baseline is much stronger | M13-30, M13-29 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md docs/CONTENT_TEXT_POLICY.md docs/NON_VERBATIM_TEXT.md` | The roadmap has an explicit post-level-4-breadth choice instead of drifting between a small spell cleanup pass, a creature pivot, and the remaining documented exact-text exception |
| M13-32 | done | M13 | Close the remaining open level 4 spell gap with the final niche SRD utility and warding entries | M13-31, M13-30 | `cmd /c npx -y -p node@22 -p npm@10 npm run content:build && cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The repo-managed SRD spell pack completes its current open level 4 baseline, the generated content build is regenerated, and regression coverage asserts the added spell lookup, exact joined description, and class-list baseline |
| M13-33 | done | M13 | Decide whether the next spell-breadth pass should open level 5 or pause spell expansion now that the open level 4 baseline is complete | M13-32, M13-31 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md docs/CONTENT_TEXT_POLICY.md docs/NON_VERBATIM_TEXT.md` | The roadmap has an explicit post-level-4 direction instead of drifting between level 5 spells, creature work the user does not prioritize, and ad hoc cleanup |
| M13-34 | done | M13 | Broaden the spell pack again with a bounded first level 5 tranche focused on iconic recovery, divination, travel, battlefield control, and other high-traffic finishers | M13-33, M13-32 | `cmd /c npx -y -p node@22 -p npm@10 npm run content:build && cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The repo-managed SRD spell pack opens level 5 with a bounded set of high-traffic recovery, divination, travel, battlefield-control, and utility staples, the generated content build is regenerated, and regression coverage asserts the broader spell lookup, exact joined description, and class-list baseline |
| M13-35 | done | M13 | Decide whether the next spell-breadth pass should stay on spells for another level 5 slice or pause spell expansion after the first level 5 tranche | M13-34, M13-33 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md docs/CONTENT_TEXT_POLICY.md docs/NON_VERBATIM_TEXT.md` | The roadmap has an explicit post-tranche direction instead of drifting between another level 5 slice, a creature pivot the user does not prioritize, and ad hoc cleanup |
| M13-36 | done | M13 | Broaden the spell pack again with a bounded second level 5 tranche focused on divination, coordination, spiritual protection, domination, binding, and primal traversal staples | M13-35, M13-34 | `cmd /c npx -y -p node@22 -p npm@10 npm run content:build && cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The repo-managed SRD spell pack deepens level 5 with another bounded set of divination, coordination, protection, binding, and traversal staples, the generated content build is regenerated, and regression coverage asserts the broader spell lookup, exact joined description, and class-list baseline |
| M13-37 | done | M13 | Decide whether the next spell-breadth pass should stay on spells for a third level 5 slice or pause spell expansion after the second level 5 tranche | M13-36, M13-35 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md docs/CONTENT_TEXT_POLICY.md docs/NON_VERBATIM_TEXT.md` | The roadmap has an explicit post-tranche direction instead of drifting between another level 5 slice, a low-priority creature pivot, and ad hoc cleanup |
| M13-38 | done | M13 | Broaden the spell pack again with a bounded third level 5 tranche focused on summoning, poison pressure, illusion, nightmare utility, and telekinetic battlefield control | M13-37, M13-36 | `cmd /c npx -y -p node@22 -p npm@10 npm run content:build && cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The repo-managed SRD spell pack deepens level 5 with another bounded set of summoning, poison, illusion, nightmare, and telekinetic control staples, the generated content build is regenerated, and regression coverage asserts the broader spell lookup, exact joined description, and class-list baseline |
| M13-39 | done | M13 | Decide whether to finish the small remaining open level 5 spell tail or pause spell expansion now that the broader level 5 baseline is materially in place | M13-38, M13-37 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md docs/CONTENT_TEXT_POLICY.md docs/NON_VERBATIM_TEXT.md` | The roadmap has an explicit post-tranche direction instead of drifting between a small final level 5 cleanup, a low-priority creature pivot, and ad hoc pause |
| M13-40 | done | M13 | Close the remaining open level 5 spell gap with the final utility, warding, resurrection, memory, and companion entries | M13-39, M13-38 | `cmd /c npx -y -p node@22 -p npm@10 npm run content:build && cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The repo-managed SRD spell pack closes its current open level 5 baseline, the generated content build is regenerated, and regression coverage asserts the added spell lookup, exact joined description, and class-list baseline |
| M13-41 | done | M13 | Decide whether to open level 6 spells next or pause spell expansion now that the current open level 5 baseline is complete | M13-40, M13-39 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md docs/CONTENT_TEXT_POLICY.md docs/NON_VERBATIM_TEXT.md` | The roadmap has an explicit post-level-5 direction instead of drifting between an immediate level 6 pass, a low-priority creature pivot, and ad hoc pause |
| M13-42 | done | M13 | Open level 6 with a bounded first spell tranche focused on iconic arcane damage, anti-magic defense, top-end healing, feast buffs, control, sight, and wall pressure | M13-41, M13-40 | `cmd /c npx -y -p node@22 -p npm@10 npm run content:build && cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The repo-managed SRD spell pack opens level 6 with a bounded set of iconic damage, defense, healing, buff, vision, and wall-control staples, the generated content build is regenerated, and regression coverage asserts the broader spell lookup, exact joined description, and class-list baseline |
| M13-43 | done | M13 | Decide whether the next spell-breadth pass should stay on level 6 for another slice or pause spell expansion after the first level 6 tranche | M13-42, M13-41 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md docs/CONTENT_TEXT_POLICY.md docs/NON_VERBATIM_TEXT.md` | The roadmap has an explicit post-tranche direction instead of drifting between another level 6 slice, a low-priority creature pivot, and ad hoc pause |
| M13-44 | done | M13 | Broaden level 6 again with a bounded second tranche focused on divine or primal traversal, warding, attrition, petrification, and terrain-control staples | M13-43, M13-42 | `cmd /c npx -y -p node@22 -p npm@10 npm run content:build && cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The repo-managed SRD spell pack deepens level 6 with another bounded set of traversal, warding, necrotic attrition, petrification, and battlefield-shaping staples, the generated content build is regenerated, and regression coverage asserts the broader spell lookup, exact joined description, and class-list baseline |
| M13-45 | done | M13 | Decide whether the next spell-breadth pass should stay on level 6 for a third slice or pause spell expansion after the second level 6 tranche | M13-44, M13-43 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md docs/CONTENT_TEXT_POLICY.md docs/NON_VERBATIM_TEXT.md` | The roadmap has an explicit post-tranche direction instead of drifting between another level 6 slice, a low-priority creature pivot, and ad hoc pause |
| M13-46 | done | M13 | Close the remaining open level 6 spell gap with the final arcane, undead, warding, extraplanar, and illusion entries | M13-45, M13-44 | `cmd /c npx -y -p node@22 -p npm@10 npm run content:build && cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The repo-managed SRD spell pack closes its current open level 6 baseline, the generated content build is regenerated, and regression coverage asserts the added spell lookup, exact joined description, and class-list baseline |
| M13-47 | done | M13 | Decide whether to open level 7 spells next or pause spell expansion now that the current open level 6 baseline is complete | M13-46, M13-45 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md docs/CONTENT_TEXT_POLICY.md docs/NON_VERBATIM_TEXT.md` | The roadmap has an explicit post-level-6 direction instead of drifting between an immediate level 7 pass, a low-priority creature pivot, and ad hoc pause |
| M13-48 | done | M13 | Open level 7 with a bounded first spell tranche focused on travel, force control, lethal finishers, and top-end recovery staples | M13-47, M13-46 | `cmd /c npx -y -p node@22 -p npm@10 npm run content:build && cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The repo-managed SRD spell pack opens level 7 with a bounded set of high-traffic travel, force-control, lethal, and recovery staples, the generated content build is regenerated, and regression coverage asserts the broader spell lookup, exact joined description, and class-list baseline |
| M13-49 | done | M13 | Decide whether the next spell-breadth pass should stay on level 7 for another slice or pause spell expansion after the first level 7 tranche | M13-48, M13-47 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md docs/CONTENT_TEXT_POLICY.md docs/NON_VERBATIM_TEXT.md` | The roadmap has an explicit post-tranche direction instead of drifting between another level 7 slice, a low-priority creature pivot, and ad hoc pause |
| M13-50 | done | M13 | Close the remaining open level 7 spell gap with the final celestial, glyph, illusion, mansion, and duplicate entries | M13-49, M13-48 | `cmd /c npx -y -p node@22 -p npm@10 npm run content:build && cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The repo-managed SRD spell pack closes its current open level 7 baseline, the generated content build is regenerated, and regression coverage asserts the added spell lookup, exact joined description, and class-list baseline |
| M13-51 | done | M13 | Decide whether to open level 8 spells next or pause spell expansion now that the current open level 7 baseline is complete | M13-50, M13-49 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md docs/CONTENT_TEXT_POLICY.md docs/NON_VERBATIM_TEXT.md` | The roadmap has an explicit post-level-7 direction instead of drifting between immediate level 8 work, a low-priority creature pivot, and ad hoc pause |
| M13-52 | done | M13 | Open level 8 with a bounded first spell tranche focused on anti-magic defense, apex control, radiant support, battlefield catastrophe, and iconic high-end finishers | M13-51, M13-50 | `cmd /c npx -y -p node@22 -p npm@10 npm run content:build && cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The repo-managed SRD spell pack opens level 8 with a bounded set of anti-magic, domination, catastrophe, radiant, maze, and finisher staples, the generated content build is regenerated, and regression coverage asserts the broader spell lookup, exact joined description, and class-list baseline |
| M13-53 | done | M13 | Decide whether the next spell-breadth pass should stay on level 8 for another slice or pause spell expansion after the first level 8 tranche | M13-52, M13-51 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md docs/CONTENT_TEXT_POLICY.md docs/NON_VERBATIM_TEXT.md` | The roadmap has an explicit post-tranche direction instead of drifting between another level 8 slice, a low-priority creature pivot, and ad hoc pause |
| M13-54 | done | M13 | Close the remaining open level 8 spell gap with the final attraction, psychic, clone, demiplane, truth, mind-shield, and tsunami entries | M13-53, M13-52 | `cmd /c npx -y -p node@22 -p npm@10 npm run content:build && cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The repo-managed SRD spell pack closes its current open level 8 baseline, the generated content build is regenerated, and regression coverage asserts the added spell lookup, exact joined description, and class-list baseline |
| M13-55 | done | M13 | Decide whether to open level 9 spells next or pause spell expansion now that the current open level 8 baseline is complete | M13-54, M13-53 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md docs/CONTENT_TEXT_POLICY.md docs/NON_VERBATIM_TEXT.md` | The roadmap has an explicit post-level-8 direction instead of drifting between immediate level 9 work, a low-priority creature pivot, and ad hoc pause |
| M13-56 | done | M13 | Close the current open level 9 spell gap by adding all remaining open level 9 spells in one pass | M13-55, M13-54 | `cmd /c npx -y -p node@22 -p npm@10 npm run content:build && cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The repo-managed SRD spell pack closes its current open level 9 baseline, the generated content build is regenerated, and regression coverage asserts the added spell lookup, exact joined description, and class-list baseline |
| M13-57 | done | M13 | Decide whether to pause spell expansion or open the next non-print roadmap priority now that the current open level 9 baseline is complete | M13-56, M13-55 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md docs/CONTENT_TEXT_POLICY.md docs/NON_VERBATIM_TEXT.md` | The roadmap has an explicit post-level-9 direction instead of drifting between more spell work without another open level target, a low-priority creature pivot, and ad hoc pause |
| M13-58 | done | M13 | Improve spell compendium browse UX with full typed spell browsing, quick filters, and richer list metadata now that the spell baseline is materially complete | M13-57, M13-56 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The compendium can browse the full spell library instead of a clipped subset, spell rows expose more immediately useful metadata in the list, and quick filters make the larger spell baseline practical to navigate without changing the exact-text policy or reopening print work |
| M13-59 | done | M13 | Decide whether the next non-print slice should deepen compendium UX further or shift to another high-value surface now that spell browsing is materially improved | M13-58, M13-57 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap has an explicit follow-up direction instead of drifting between more spell UX polish, a lower-priority creature/content pivot, and unrelated surface work |
| M13-60 | done | M13 | Make compendium browse state URL-stable so search and spell filters survive refresh/share and result selection preserves the active browse context | M13-59, M13-58 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The compendium browse state is now URL-driven instead of ephemeral, spell filters and queries survive refresh/share, selecting a result preserves the current browse context, and a bounded reset control returns the page to a clean browse baseline |
| M13-61 | done | M13 | Decide whether the next non-print slice should keep deepening compendium UX or shift that momentum into another high-value surface now that spell browse state is URL-stable | M13-60, M13-59 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly pivots from compendium-only polish to builder spell UX instead of drifting between more compendium work, unrelated builder work, and lower-priority content pivots |
| M13-62 | done | M13 | Replace the builder's flat class-spell checkbox wall with a grouped spell browser that supports search, level/school filters, and selected/ready browse modes | M13-61, M13-60 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The builder's spell workflow now groups spells by level, supports in-list search and narrowing, exposes richer spell metadata, preserves a dedicated ready list, and makes the main spell section wide enough to use against the current full spell baseline without changing print direction or source-text policy |
| M13-63 | done | M13 | Decide whether the next non-print slice should deepen builder spell UX further or shift to cross-surface reference handoff now that builder spell browse is no longer a flat checkbox list | M13-62, M13-61 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly shifts from builder spell-list polish to cross-surface compendium handoff instead of drifting between more builder-only tweaks, more compendium-only tweaks, and lower-priority surface pivots |
| M13-64 | done | M13 | Preserve character context when opening the full compendium from the builder or saved sheet, and surface a bounded return action inside the compendium | M13-63, M13-62 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | Opening the full compendium from a linked builder or sheet reference now carries typed entry context plus a return path, the compendium exposes a clear back-to-builder or back-to-sheet action that survives local browse-state changes, and the local reference panel no longer feels disconnected from the full compendium surface |
| M13-65 | done | M13 | Decide whether the next non-print slice should deepen cross-surface reference UX further or return to builder/compendium polish now that full-compendium handoff preserves character context | M13-64, M13-63 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly stays on cross-surface reference UX for one more bounded slice instead of drifting between more navigation polish, more local builder tweaks, and unrelated lower-priority pivots |
| M13-66 | done | M13 | Add detail-side related and adjacent compendium navigation that preserves the builder or sheet return handoff while browsing linked references | M13-65, M13-64 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The full compendium detail view now exposes adjacent-result navigation plus explicit related-entry jumps, those jumps preserve the current character return handoff, and users can move across linked rules/items without feeling trapped in a dead-end detail page |
| M13-67 | done | M13 | Decide whether the next non-print slice should keep deepening cross-surface reference UX or return to builder/compendium polish now that detail-side related navigation exists | M13-66, M13-65 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly returns to local builder polish instead of drifting between a third cross-surface navigation pass, more compendium-only work, and unrelated lower-priority pivots |
| M13-68 | done | M13 | Replace the builder's flat feat checkbox wall with a browsable feat selector that supports search, support-level narrowing, and selected-state browse modes | M13-67, M13-66 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The builder's feat workflow no longer relies on a flat checkbox wall, in-list search and support-level narrowing make the current feat baseline practical to browse, blocked feat constraints remain visible in context, and the feat surface now matches the newer spell-selection UX more closely without reopening print or source-text scope |
| M13-69 | done | M13 | Decide whether the next non-print slice should keep deepening local builder or compendium polish or shift to another surface now that feat selection is no longer a flat checkbox wall | M13-68, M13-67 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly stays on local compendium polish for one more bounded slice instead of drifting between more builder tweaks, more cross-surface navigation work, and unrelated lower-priority pivots |
| M13-70 | done | M13 | Add generic typed quick filters and richer browse metadata for non-spell compendium views so feats, creatures, rules, and equipment are easier to scan without raw text search alone | M13-69, M13-68 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | Typed non-spell compendium views now expose bounded quick filters from existing metadata, list rows surface more immediately useful context than source labels alone, and the broader compendium becomes easier to browse without reopening print work, content-schema work, or source-text scope |
| M13-71 | done | M13 | Decide whether the next non-print slice should keep deepening local compendium polish or shift to another surface now that typed non-spell views have quick filters and richer browse metadata | M13-70, M13-69 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly shifts from compendium polish to builder equipment or inventory UX instead of drifting between more browse tweaks, more cross-surface navigation work, and unrelated lower-priority pivots |
| M13-72 | done | M13 | Replace the builder's add-equipment chip wall with a searchable equipment browser that exposes type, category, and tracked or equipped state | M13-71, M13-70 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | Builder equipment selection now scales beyond flat chip walls, users can find items by type, category, or rules text cues, tracked or equipped state is visible before adding another copy, and the builder's equipment surface now matches the newer spell and feat browse patterns more closely without reopening print or source-text scope |
| M13-73 | done | M13 | Decide whether the next non-print slice should keep deepening builder equipment or inventory UX or shift to another surface now that adding equipment is no longer a flat chip wall | M13-72, M13-71 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly stays on builder inventory UX for one more bounded slice instead of drifting between more add-equipment polish, more compendium work, and unrelated lower-priority pivots |
| M13-74 | done | M13 | Replace the builder's flat tracked-inventory list with a browsable inventory manager that supports search, kind/category filters, and equipped-state browsing | M13-73, M13-72 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The builder's tracked inventory no longer requires scanning one flat list, users can narrow to specific item kinds or equipped state, quantity/equip/reference/remove actions remain in place inside the filtered view, and the full equipment workflow now stays consistent from adding items through managing them |
| M13-75 | done | M13 | Decide whether the next non-print slice should keep deepening builder inventory/loadout UX or shift to another surface now that tracked inventory is no longer a flat list | M13-74, M13-73 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly stays on builder loadout UX for one more bounded slice instead of drifting between more general inventory polish, more compendium work, and unrelated lower-priority pivots |
| M13-76 | done | M13 | Add a focused loadout manager above the broader inventory so equipped armor, shield, and active weapons can be changed directly from one summary surface | M13-75, M13-74 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | Equipped armor, shield, and active weapons no longer require drilling into the broader inventory manager to change them, the builder now exposes a clearer top-level loadout surface for combat-relevant gear, and the full equipment workflow stays coherent from adding items through activating them |
| M13-77 | done | M13 | Decide whether the next non-print slice should keep deepening builder loadout/inventory UX or shift to another surface now that equipped armor, shield, and active weapons have a focused manager | M13-76, M13-75 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly shifts away from builder equipment/loadout polish toward the remaining builder feat-configuration surface instead of drifting between more inventory tweaks, more compendium work, and unrelated lower-priority pivots |
| M13-78 | done | M13 | Replace the builder's ad hoc configurable-feat checkbox cards with a feat-choice browser that supports search, clearer group state, and unavailable-choice visibility | M13-77, M13-68 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | Configurable feat choices no longer rely on one-off checkbox stacks, users can search and inspect grouped feat options with clearer selected or unavailable state, and the feat workflow now stays coherent from picking a feat through configuring its required choices |
| M13-79 | done | M13 | Decide whether the next non-print slice should keep deepening builder configuration UX or shift to another surface now that configurable feat choices no longer rely on ad hoc checkbox cards | M13-78, M13-77 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly shifts from local builder polish into saved-sheet automation work instead of drifting between more builder tweaks, more compendium work, and unrelated lower-priority pivots |
| M13-80 | done | M13 | Add a first saved-sheet automation pass for class resource charges so features like Rage and Wild Shape appear automatically and can be spent directly from the sheet | M13-79, M13-76 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | Saved-sheet automation no longer depends entirely on manually configured resource rows, supported class resources like Rage and Wild Shape are generated automatically with rest-aware recovery, and players can spend or restore those charges directly from the saved-sheet route |
| M13-81 | done | M13 | Decide whether the next non-print slice should keep expanding saved-sheet automation or stop after the first class-resource pass | M13-80, M13-79 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly stays on saved-sheet automation for one more bounded slice instead of drifting between more class-resource automation, more builder polish, and unrelated lower-priority pivots |
| M13-82 | done | M13 | Expand saved-sheet automation to more high-frequency class resources that fit the current counter model cleanly | M13-81, M13-80 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | Saved-sheet automation now also auto-generates Bardic Inspiration, Cleric and Paladin Channel Divinity, Fighter Second Wind and Action Surge, and Paladin Lay on Hands with the right bounded rest recovery, while live sheet controls still let players spend or restore those resources directly without manual setup |
| M13-83 | done | M13 | Decide whether the next non-print slice should keep expanding saved-sheet automation after the second class-resource pass or stop here | M13-82, M13-81 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly stays on saved-sheet automation for one more bounded slice instead of drifting between more class-resource automation, another surface pivot, and unrelated lower-priority work |
| M13-84 | done | M13 | Add direct saved-sheet spell-slot controls and the remaining obvious magic-resource trackers for the current class baseline | M13-83, M13-82 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The saved-sheet route now lets players spend or restore tracked standard and pact spell slots directly, and it auto-generates Wizard Arcane Recovery, Sorcerer Sorcery Points, and Warlock Magical Cunning alongside the earlier class-resource rows without requiring manual tracker setup |
| M13-85 | done | M13 | Decide whether the next non-print slice should keep expanding saved-sheet automation after the spell-slot and magic-resource pass or stop here | M13-84, M13-83 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly stays on saved-sheet automation for one more bounded slice instead of pivoting away after the spell-slot and magic-resource pass |
| M13-86 | done | M13 | Expand saved-sheet automation to the remaining obvious long-rest class counters that fit the current tracked-resource model | M13-85, M13-84 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | Saved-sheet automation now also auto-generates Cleric Divine Intervention, Fighter Indomitable, Ranger Favored Enemy, Sorcerer Innate Sorcery, and Warlock Contact Patron so more at-table counters work without manual tracker setup while deeper recovery systems stay explicitly out of scope |
| M13-87 | done | M13 | Decide whether the next non-print slice should keep expanding saved-sheet automation after the fourth class-resource pass or stop before opening a deeper rest-feature system | M13-86, M13-85 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly stays on saved-sheet automation for one more bounded slice instead of pivoting away after the fourth class-resource pass |
| M13-88 | done | M13 | Expand saved-sheet automation to the remaining bounded class-rest counters that still fit the current tracked-resource model cleanly | M13-87, M13-86 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | Saved-sheet automation now also auto-generates Druid Wild Resurgence, Ranger Tireless, Ranger Nature's Veil, and Sorcerer Sorcerous Restoration alongside the earlier class-resource rows so more at-table rest counters work without inventing a deeper feature engine |
| M13-89 | done | M13 | Decide whether the next non-print slice should keep expanding saved-sheet automation after the fifth class-resource pass or stop before opening subclass or deeper recovery automation | M13-88, M13-87 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly stays on saved-sheet automation for one more bounded slice instead of pivoting away after the fifth class-resource pass |
| M13-90 | done | M13 | Add the last obvious general-class counters that still fit the current tracked-resource model without subclass logic | M13-89, M13-88 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | Saved-sheet automation now also auto-generates Paladin Faithful Steed and Rogue Stroke of Luck so the last obvious general-class counters are available at the table without inventing subclass-specific or deeper mutation automation |
| M13-91 | done | M13 | Decide whether to stop saved-sheet automation here or reopen the scope around subclass-specific or deeper recovery and mutation features | M13-90, M13-89 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly reopens automation around narrow subclass-aware counters instead of pretending the next step should be a deeper recovery engine immediately |
| M13-92 | done | M13 | Add a first subclass-aware saved-sheet automation pass for the clearest counter-based subclass resources in the current baseline | M13-91, M13-90 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | Saved-sheet automation now also auto-generates Battle Master Superiority Dice and Light Domain Warding Flare so the first subclass-specific counter features work at the table without opening a broader subclass or mutation engine |
| M13-93 | done | M13 | Decide whether to stop automation after the first subclass-aware pass or reopen scope around more subclass-specific or deeper mutation features | M13-92, M13-91 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly stays on one more narrow subclass-aware automation slice instead of jumping straight into a broader subclass or mutation engine |
| M13-94 | done | M13 | Add the next clearest open subclass counter that still fits the current tracked-resource model without deeper slot-mutation automation | M13-93, M13-92 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | Saved-sheet automation now also auto-generates Circle of the Land Natural Recovery so another supported open subclass rest feature is available at the table without opening a broader subclass or spell-slot mutation engine |
| M13-95 | done | M13 | Decide whether to stop automation after the second subclass-aware pass or reopen scope around more subclass-specific or deeper mutation features | M13-94, M13-93 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly reopens bounded action-level recovery and mutation automation instead of drifting between more counter work, another unrelated surface pivot, or a premature stop |
| M13-96 | done | M13 | Add a first bounded saved-sheet recovery-action pass for existing magic features that mutate slots or pools directly | M13-95, M13-94 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The saved-sheet spellbook page now supports direct recovery actions for Arcane Recovery, Natural Recovery, Magical Cunning, and Sorcerous Restoration, with bounded slot-selection or one-click restoration flows instead of only passive counters and notes |
| M13-97 | done | M13 | Decide whether to keep expanding deeper recovery and mutation automation after the first action-level pass or stop here | M13-96, M13-95 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly stays on one more bounded pool-spender pass instead of stopping after the first action-level recovery slice or drifting back to unrelated surface work |
| M13-98 | done | M13 | Add a first bounded saved-sheet pool-spender pass for tracked counter resources that need multi-point adjustment at the table | M13-97, M13-96 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The saved-sheet tracked-resource panel now supports direct multi-point spend or restore controls for larger counter pools, which makes Lay on Hands and Sorcery Points practical to manage live without repeated single-step clicks |
| M13-99 | done | M13 | Decide whether to keep expanding deeper mutation automation after the first pool-spender pass or stop here | M13-98, M13-97 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly stays on one more bounded hit-die action slice instead of stopping after the first pool-spender pass or drifting into a broader rest engine |
| M13-100 | done | M13 | Add a first bounded saved-sheet hit-die action pass for short-rest healing without inventing a full dice-roller engine | M13-99, M13-98 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The saved-sheet rest panel now supports direct Hit Dice spending plus a bounded average-heal shortcut, so players can manage short-rest healing in-app without pretending the route is a full die-by-die rest simulator |
| M13-101 | done | M13 | Decide whether to keep expanding deeper mutation automation after the first hit-die action pass or stop here | M13-100, M13-99 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly stays on one more bounded direct-healing workflow instead of stopping after the hit-die pass or drifting into a broader rest engine |
| M13-102 | done | M13 | Add a first bounded direct healing-pool workflow on the saved sheet without pretending the app now models every target-side effect | M13-101, M13-100 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The saved-sheet tracked-resource panel now lets Paladins apply Lay on Hands healing directly to the current character HP pool using the existing amount controls, while condition removal and broader target-side semantics remain manual instead of being approximated badly |
| M13-103 | done | M13 | Decide whether to keep expanding deeper mutation automation after the first direct healing-pool workflow or stop here | M13-102, M13-101 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly stays on one more bounded self-only action workflow instead of jumping into broader target-side pool semantics or stopping automation outright |
| M13-104 | done | M13 | Add a first bounded self-only class-heal workflow that uses the existing saved-sheet resource row instead of opening a broader action engine | M13-103, M13-102 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The saved-sheet tracked-resource panel now lets Fighters trigger Second Wind self-healing directly with a bounded average-heal shortcut, while Tactical Mind and Tactical Shift remain manual instead of being approximated badly |
| M13-105 | done | M13 | Decide whether to keep expanding deeper mutation automation after the first self-only class-heal workflow or stop here | M13-104, M13-103 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly stays on one more bounded self-only temporary-HP workflow instead of jumping into broader target semantics or stopping automation outright |
| M13-106 | done | M13 | Add a first bounded self-only temporary-HP workflow that uses the existing saved-sheet resource row instead of opening a broader effect engine | M13-105, M13-104 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The saved-sheet tracked-resource panel now lets Rangers trigger Tireless temporary HP directly with a bounded average shortcut, while exhaustion reduction remains manual instead of being approximated badly |
| M13-107 | done | M13 | Decide whether to keep expanding deeper mutation automation after the first self-only temporary-HP workflow or stop here | M13-106, M13-105 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly stops the deeper mutation branch at the current bounded layer and pivots to saved-sheet usability polish instead of drifting into broader target semantics or another unrelated surface |
| M13-108 | done | M13 | Add a first saved-sheet usability polish pass that triages tracked resources now that the play-state surface carries many more automated rows | M13-107, M13-106 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The saved-sheet tracked-resource surface now shows needs-attention rows first, collapses full ready rows behind an explicit reveal on page 1, and keeps page 2 focused on changed resources instead of forcing the player to scan one long flat list |
| M13-109 | done | M13 | Decide whether to keep deepening saved-sheet usability polish after the first tracked-resource triage pass or pivot to another non-print surface | M13-108, M13-107 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly stays on saved-sheet usability for one more bounded handoff pass instead of drifting back into deeper automation or another unrelated surface |
| M13-110 | done | M13 | Add a saved-sheet recovery handoff pass so page-one rest flow surfaces page-two recovery readiness instead of hiding it behind another page | M13-109, M13-108 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The saved-sheet rest panel now surfaces recovery highlights on page 1 and links directly into the page-two recovery section, so spell-slot and pool recovery readiness no longer feels disconnected from the rest controls |
| M13-111 | done | M13 | Decide whether to keep deepening saved-sheet usability polish after the first recovery handoff pass or pivot to another non-print surface | M13-110, M13-109 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly stays on saved-sheet usability for one more bounded spellbook pass instead of drifting back into deeper automation or another unrelated surface |
| M13-112 | done | M13 | Add a saved-sheet spellbook browse/filter pass so page two scales better without becoming another compendium surface | M13-111, M13-110 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The saved-sheet spellbook page now supports local search plus bounded All/Prepared/Cantrips/Leveled filters, and the inspector stays aligned to the visible filtered rows instead of getting stuck on hidden spells |
| M13-113 | done | M13 | Decide whether to keep deepening saved-sheet usability polish after the first spellbook browse/filter pass or pivot to another non-print surface | M13-112, M13-111 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly stays on saved-sheet usability for one more bounded spell-detail navigation slice instead of drifting back into deeper automation or unrelated surface work |
| M13-114 | done | M13 | Add a saved-sheet spell-detail navigation pass so page-two inspection works within the current filtered spellbook rows | M13-113, M13-112 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The saved-sheet spell inspector now exposes bounded previous/next navigation plus visible row-position context inside the current filtered spell table, so page-two browsing no longer depends entirely on hover or pinning |
| M13-115 | done | M13 | Decide whether to keep deepening saved-sheet usability polish after the spell-detail navigation pass or pivot to another non-print surface | M13-114, M13-113 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly pivots from saved-sheet polish to a bounded character-creation onboarding slice instead of silently stretching the saved-sheet branch |
| M13-116 | done | M13 | Add class-based standard ability arrays to the character builder so new drafts can apply a quick-start score spread per class | M13-115, M13-114 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The character builder now exposes app-authored class standard arrays based on the shared `15 / 14 / 13 / 12 / 10 / 8` spread, auto-carries them across fresh class changes, and lets the player reapply the current class recommendation with one click |
| M13-117 | done | M13 | Decide whether to keep deepening builder onboarding after the first class standard-array pass or pivot to another non-print surface | M13-116, M13-115 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly stays on builder onboarding for one more bounded quick-start setup slice instead of drifting between more builder tweaks and unrelated surfaces |
| M13-118 | done | M13 | Add a combined class-and-background quick-start setup action to the builder so new drafts can apply core onboarding recommendations in one step | M13-117, M13-116 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The builder now exposes a one-click quick-start action that applies the current class standard array plus the current background's suggested skills and seeded starting gear, while preserving the separate background and ability controls for manual adjustment |
| M13-119 | done | M13 | Decide whether to keep deepening builder onboarding after the quick-start setup pass or pivot to another non-print surface | M13-118, M13-117 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly stays on builder onboarding for one more bounded starter-spell slice instead of silently stretching the quick-start branch or pivoting early |
| M13-120 | done | M13 | Add class-based starter spell recommendations to the builder so caster drafts can apply a bounded seeded opening spell package | M13-119, M13-118 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The builder now auto-carries app-authored starter spell packages across fresh class changes, exposes an explicit starter-spell apply action, and folds those seeded spell recommendations into the broader quick-start setup path without silently overwriting manual spell edits |
| M13-121 | done | M13 | Decide whether to keep deepening builder onboarding after the starter-spell pass or pivot to another non-print surface | M13-120, M13-119 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly stays on builder onboarding for one more bounded starter-skill slice instead of silently stretching the starter-spell branch or pivoting early |
| M13-122 | done | M13 | Add class-based starter skill recommendations to the builder so class changes and quick-start setup no longer inherit stale early-draft skill defaults | M13-121, M13-120 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The builder now auto-carries app-authored starter skill packages across fresh class changes, exposes an explicit starter-skill apply action, and folds those skill recommendations into the broader quick-start setup path without silently overwriting later manual skill edits |
| M13-123 | done | M13 | Decide whether to keep deepening builder onboarding after the starter-skill pass or pivot to another non-print surface | M13-122, M13-121 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly pivots from builder onboarding to a bounded roster-usability slice instead of silently stretching the creation-flow branch |
| M13-124 | done | M13 | Add a bounded roster browse pass so the launch page scales better than a flat recent-only character list | M13-123, M13-122 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The character roster now supports local search, class/species filters, sort controls, resettable browse state, and a filtered empty state without changing persistence or route structure |
| M13-125 | done | M13 | Decide whether to keep deepening roster usability after the first browse/filter/sort pass or pivot to another non-print surface | M13-124, M13-123 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap explicitly stays on roster usability for one more bounded launch-page management slice instead of pivoting away immediately after the first browse pass |
| M13-126 | done | M13 | Add launch-page roster management actions so export and deletion do not require leaving the roster first | M13-125, M13-124 | `cmd /c npx -y -p node@22 -p npm@10 npm run typecheck && cmd /c npx -y -p node@22 -p npm@10 npm run test && cmd /c npx -y -p node@22 -p npm@10 npm run lint && cmd /c npx -y -p node@22 -p npm@10 npm run build:win-local` | The character roster now supports per-card JSON export and delete actions with inline action state and message feedback, so launch-page management no longer requires a detour into the sheet or builder route |
| M13-127 | pending | M13 | Decide whether to keep deepening roster usability after the launch-page management pass or pivot to another non-print surface | M13-126, M13-125 | `git diff -- docs/PLAN.md docs/CHECKLIST.md docs/STATUS.md docs/DECISIONS.md` | The roadmap has an explicit post-roster-management direction instead of silently stretching the roster branch |
